const sum = (um: number, dois: number): number => {
    return um + dois;
};

const sumLambda = (um: number, dois: number): number => um + dois;

console.log(sum(1, 2));

console.log('sumLambda', sumLambda(1, 2));
